# Keep the published archive lean. A .pubignore takes precedence over
# .gitignore, so build artifacts and dev-only files must be listed explicitly.

build/
.dart_tool/
implementasi_plan.md

# The Node.js test server is a dev aid, not part of the Dart package.
example/server/

# Example build output and generated platform folders.
example/.dart_tool/
example/android/
example/ios/
example/linux/
example/macos/
example/windows/
example/web/
example/build/
